#! /bin/sh

if [ ! -e /etc/pdevs.key ]; then
    dd if=/dev/random of=/etc/pdevs.key bs=1 count=256
    chown root:root /etc/pdevs.key
    chmod 400 /etc/pdevs.key
fi

update-initramfs -u

#DEBHELPER#
