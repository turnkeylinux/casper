#!/bin/sh

PREREQ=""

prereqs()
{
       echo "$PREREQ"
}

case $1 in
# get pre-requisites
prereqs)
       prereqs
       exit 0
       ;;
esac

grep integrity-check /proc/cmdline && casper-md5check ${mountpoint} ${mountpoint}/md5sum.txt < /dev/tty8 
