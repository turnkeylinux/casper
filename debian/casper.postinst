#! /bin/sh

embedded_key="/etc/embedded.key"

if [ ! -e ${embedded_key} ]; then
    dd if=/dev/random of=${embedded_key} bs=1 count=256
    chown root:root ${embedded_key}
    chmod 400 ${embedded_key}
fi

update-initramfs -u

#DEBHELPER#
